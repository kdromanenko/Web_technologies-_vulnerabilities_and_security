Для получения файла с помощью команды curl, попробуйте команду: curl -u admin:pass http://localhost/file.txt .
Для получения файла с помощью команды telnet, попробуйте команду:
echo 'login:pass' | openssl base64 -base64
telnet localhost 80
Authorization: Basic <хэш, полученный в первом шаге>
